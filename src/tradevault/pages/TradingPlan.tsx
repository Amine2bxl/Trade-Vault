import { useState } from 'react';
import { Plus, Save, Trash2 } from 'lucide-react';
import { loadTradingPlan, saveTradingPlan } from '../store';
import { useT } from '../i18n/LanguageContext';
import type { TradingPlan } from '../types';
export default function TradingPlan() { const { t } = useT(); const [plan, setPlan] = useState<TradingPlan>(() => loadTradingPlan()); const update = (key: keyof TradingPlan, value: string | number | string[]) => setPlan((current) => ({ ...current, [key]: value })); const save = () => { saveTradingPlan(plan); }; const addRule = () => update('rules', [...plan.rules, '']); const removeRule = (index: number) => update('rules', plan.rules.filter((_, i) => i !== index)); return <div className="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-10 space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Process</p><h1 className="mt-2 text-3xl md:text-4xl font-bold text-white">{t('nav.tradingPlan')}</h1><p className="mt-2 text-sm text-slate-400">Turn your edge into a repeatable operating system.</p></div><section className="glass-strong rounded-3xl p-5 md:p-6 space-y-5"><div className="grid gap-4 md:grid-cols-2"><label className="text-xs text-slate-400">Market<input value={plan.market} onChange={(e) => update('market', e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2.5 text-sm text-white" /></label><label className="text-xs text-slate-400">Session<input value={plan.session} onChange={(e) => update('session', e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2.5 text-sm text-white" /></label><label className="text-xs text-slate-400">Risk per trade (%)<input type="number" value={plan.riskPerTrade} onChange={(e) => update('riskPerTrade', Number(e.target.value))} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2.5 text-sm text-white" /></label><label className="text-xs text-slate-400">Max daily loss (%)<input type="number" value={plan.maxDailyLoss} onChange={(e) => update('maxDailyLoss', Number(e.target.value))} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2.5 text-sm text-white" /></label></div><div><div className="flex items-center justify-between"><h2 className="text-sm font-bold text-white">Rules</h2><button onClick={addRule} className="inline-flex items-center gap-1 text-xs font-semibold text-cyan-300"><Plus className="h-4 w-4" />Add rule</button></div><div className="mt-3 space-y-2">{plan.rules.map((rule, index) => <div key={index} className="flex gap-2"><input value={rule} onChange={(e) => update('rules', plan.rules.map((item, i) => i === index ? e.target.value : item))} className="min-w-0 flex-1 rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2.5 text-sm text-white" /><button onClick={() => removeRule(index)} className="rounded-xl border border-white/10 px-3 text-slate-400"><Trash2 className="h-4 w-4" /></button></div>)}</div></div><div className="flex justify-end"><button onClick={save} className="inline-flex items-center gap-2 rounded-xl bg-cyan-400 px-4 py-2.5 text-sm font-bold text-slate-950"><Save className="h-4 w-4" />Save plan</button></div></section></div>; }
