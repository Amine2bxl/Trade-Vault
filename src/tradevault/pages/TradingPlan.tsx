import { useEffect, useState } from 'react';
import { Save } from 'lucide-react';
import { toast } from 'sonner';
import type { TradingPlan } from '../types';
import { DEFAULT_TRADING_PLAN } from '../planning';
import { loadTradingPlan, saveTradingPlan } from '../store';
export default function TradingPlan() { const [plan, setPlan] = useState<TradingPlan>(DEFAULT_TRADING_PLAN); useEffect(() => setPlan(loadTradingPlan()), []); const update = (patch: Partial<TradingPlan>) => setPlan((current) => ({ ...current, ...patch })); const save = () => { saveTradingPlan(plan); toast.success('Trading plan saved'); }; return <div className="mx-auto max-w-4xl space-y-6 px-4 py-6 sm:px-6 sm:py-10"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Planning</p><h1 className="mt-2 text-3xl font-bold text-white">Trading Plan</h1><p className="mt-2 text-sm text-slate-400">Turn your selected goals into rules you can actually follow.</p></div><button onClick={save} className="flex items-center gap-2 rounded-xl bg-cyan-400 px-3 py-2 text-sm font-semibold text-slate-950"><Save className="h-4 w-4" />Save</button></div><section className="glass-strong rounded-3xl p-5 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm text-slate-400">Account risk per trade<input type="number" value={plan.riskPerTrade} onChange={(e) => update({ riskPerTrade: Number(e.target.value) })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-white outline-none" /></label><label className="text-sm text-slate-400">Max trades per day<input type="number" value={plan.maxTradesPerDay} onChange={(e) => update({ maxTradesPerDay: Number(e.target.value) })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-white outline-none" /></label><label className="text-sm text-slate-400">Daily loss limit<input type="number" value={plan.dailyLossLimit} onChange={(e) => update({ dailyLossLimit: Number(e.target.value) })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-white outline-none" /></label><label className="text-sm text-slate-400">Minimum R:R<input type="number" step="0.1" value={plan.minRiskReward} onChange={(e) => update({ minRiskReward: Number(e.target.value) })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-white outline-none" /></label></div><label className="mt-5 block text-sm text-slate-400">Market / setup notes<textarea value={plan.notes} onChange={(e) => update({ notes: e.target.value })} className="mt-2 min-h-32 w-full rounded-xl border border-white/10 bg-white/[0.04] p-3 text-sm text-white outline-none" /></label></section></div>; }
