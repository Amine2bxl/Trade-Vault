import { useEffect, useState } from 'react';
import { ClipboardCheck, Save } from 'lucide-react';
import { toast } from 'sonner';
import { loadTradingPlan, saveTradingPlan } from '../store';
export default function TradingPlan() { const [plan, setPlan] = useState(loadTradingPlan()); useEffect(() => saveTradingPlan(plan), [plan]); const update = (key: keyof typeof plan, value: string) => setPlan({ ...plan, [key]: value }); return <div className="mx-auto max-w-4xl space-y-6 px-4 py-6 sm:px-6 sm:py-10"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Process</p><h1 className="mt-2 text-3xl font-bold text-white">Trading Plan</h1><p className="mt-2 text-sm text-slate-400">Keep your rules visible before the market starts moving.</p></div><section className="glass-strong space-y-5 rounded-3xl p-5 sm:p-7">{[['thesis', 'Market thesis'], ['setups', 'Setups I trade'], ['riskRules', 'Risk rules'], ['exitRules', 'Exit rules'], ['noTradeRules', 'No-trade rules']].map(([key, label]) => <label key={key} className="block text-sm text-slate-400">{label}<textarea value={plan[key as keyof typeof plan]} onChange={(e) => update(key as keyof typeof plan, e.target.value)} rows={3} className="mt-2 w-full rounded-2xl border border-white/10 bg-white/[0.03] p-3 text-sm text-white outline-none focus:border-cyan-300/50" /></label>)}<button onClick={() => { saveTradingPlan(plan); toast.success('Trading plan saved'); }} className="flex items-center gap-2 rounded-xl bg-cyan-400 px-4 py-2 font-semibold text-slate-950"><Save className="h-4 w-4" />Save plan</button></section><div className="flex items-center gap-2 text-xs text-slate-500"><ClipboardCheck className="h-4 w-4 text-cyan-300" />Your plan is saved locally and stays available between sessions.</div></div>; }
