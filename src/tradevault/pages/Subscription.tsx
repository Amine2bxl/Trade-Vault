import { Check, CreditCard } from 'lucide-react';
const plans = [{ name: 'Free', price: '€0', features: ['Journal', 'Goals', 'Basic analytics'] }, { name: 'Pro', price: '€19', features: ['Everything in Free', 'AI insights', 'Monthly reports', 'CSV import'] }, { name: 'Elite', price: '€39', features: ['Everything in Pro', 'Advanced reports', 'Priority support', 'Unlimited history'] }];
export default function Subscription() { return <div className="mx-auto max-w-5xl space-y-6 px-4 py-6 sm:px-6 sm:py-10"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Account</p><h1 className="mt-2 text-3xl font-bold text-white">Subscription</h1><p className="mt-2 text-sm text-slate-400">Choose the level of coaching and analysis that fits your trading.</p></div><div className="grid gap-4 md:grid-cols-3">{plans.map((plan, index) => <section key={plan.name} className={`glass-strong rounded-3xl p-5 ${index === 1 ? 'border-cyan-300/50' : ''}`}><div className="flex items-center justify-between"><h2 className="text-lg font-semibold text-white">{plan.name}</h2>{index === 1 && <span className="rounded-full bg-cyan-300 px-2 py-1 text-[10px] font-bold text-slate-950">Popular</span>}</div><p className="mt-4 text-3xl font-bold text-white">{plan.price}<span className="text-sm font-normal text-slate-500">/month</span></p><ul className="mt-5 space-y-3">{plan.features.map((feature) => <li key={feature} className="flex gap-2 text-sm text-slate-300"><Check className="h-4 w-4 shrink-0 text-cyan-300" />{feature}</li>)}</ul><button className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-sm font-semibold text-white"><CreditCard className="h-4 w-4" />Choose plan</button></section>)}</div></div>; }
