import { Check, CreditCard } from 'lucide-react';
const plans = [{ name: 'Free', price: '€0', features: ['50 journal entries', 'Core analytics', 'CSV export'] }, { name: 'Pro', price: '€19', features: ['Unlimited journal', 'AI insights', 'Monthly reports', 'Push notifications'] }, { name: 'Elite', price: '€39', features: ['Everything in Pro', 'Advanced reporting', 'Priority support'] }];
export default function Subscription() { return <div className="max-w-5xl mx-auto px-4 sm:px-6 py-6 sm:py-10 space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Billing</p><h1 className="mt-2 text-3xl font-bold text-white">Subscription</h1><p className="mt-2 text-sm text-slate-400">Choose the plan that matches the seriousness of your process.</p></div><div className="grid gap-4 md:grid-cols-3">{plans.map((plan, index) => <section key={plan.name} className={`glass-strong rounded-3xl p-5 md:p-6 ${index === 1 ? 'border-cyan-300/40 ring-1 ring-cyan-300/20' : ''}`}><div className="flex items-center justify-between"><h2 className="text-lg font-bold text-white">{plan.name}</h2>{index === 1 && <span className="rounded-full bg-cyan-300 px-2 py-1 text-[10px] font-bold text-slate-950">POPULAR</span>}</div><div className="mt-5 text-3xl font-bold text-white">{plan.price}<span className="text-sm font-normal text-slate-500"> / month</span></div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-cyan-400 px-3 py-2.5 text-sm font-bold text-slate-950"><CreditCard className="h-4 w-4" />Choose {plan.name}</button><ul className="mt-6 space-y-3">{plan.features.map((feature) => <li key={feature} className="flex items-center gap-2 text-sm text-slate-300"><Check className="h-4 w-4 text-cyan-300" />{feature}</li>)}</ul></section>)}</div></div>; }
