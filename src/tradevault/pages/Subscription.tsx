import { Check } from 'lucide-react';
const plans = [{ name: 'Free', price: '€0', features: ['Journal', 'Calendar', 'Basic analytics'] }, { name: 'Pro', price: '€12', features: ['Everything in Free', 'AI insights', 'Monthly reports', 'Unlimited goals'] }];
export default function Subscription() { return <div className="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-10 space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Account</p><h1 className="mt-2 text-3xl font-bold text-white">Subscription</h1><p className="mt-2 text-sm text-slate-400">Choose the workspace that fits your trading process.</p></div><div className="grid gap-4 md:grid-cols-2">{plans.map((plan, index) => <section key={plan.name} className={`glass-strong rounded-3xl p-5 md:p-6 ${index === 1 ? 'border-cyan-300/40' : ''}`}><p className="text-sm font-semibold text-cyan-300">{plan.name}</p><p className="mt-3 text-3xl font-bold text-white">{plan.price}<span className="text-sm font-normal text-slate-500"> / month</span></p><ul className="mt-6 space-y-3">{plan.features.map((feature) => <li key={feature} className="flex items-center gap-2 text-sm text-slate-300"><Check className="h-4 w-4 text-emerald-300" />{feature}</li>)}</ul><button className="mt-7 w-full rounded-xl border border-white/10 px-4 py-2.5 text-sm font-semibold text-white">Current plan</button></section>)}</div></div>; }
