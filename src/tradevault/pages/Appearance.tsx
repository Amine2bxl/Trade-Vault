import { useState } from 'react';
import { Check, Palette } from 'lucide-react';
import { loadAppearance, saveAppearance } from '../store';
const themes = [{ key: 'blue', label: 'Ocean blue', color: '#22d3ee' }, { key: 'violet', label: 'Violet', color: '#a78bfa' }, { key: 'cyan', label: 'Cyan', color: '#2dd4bf' }] as const;
export default function Appearance() { const [appearance, setAppearance] = useState(loadAppearance()); const update = (key: keyof typeof appearance, value: string) => { const next = { ...appearance, [key]: value }; setAppearance(next); saveAppearance(next); document.documentElement.dataset.theme = next.theme; }; return <div className="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-10 space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-cyan-400">TradeVault / Interface</p><h1 className="mt-2 text-3xl font-bold text-white">Appearance</h1><p className="mt-2 text-sm text-slate-400">Tune the visual system without changing your trading data.</p></div><section className="glass-strong rounded-3xl p-5 md:p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-2xl bg-cyan-400/10 text-cyan-300"><Palette className="h-5 w-5" /></div><div><h2 className="font-bold text-white">Theme</h2><p className="text-sm text-slate-400">Choose an accent for the entire workspace.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{themes.map((theme) => <button key={theme.key} onClick={() => update('theme', theme.key)} className={`flex items-center justify-between rounded-2xl border p-4 text-left ${appearance.theme === theme.key ? 'border-cyan-300/60 bg-cyan-300/10' : 'border-white/10 bg-white/[0.03]'}`}><span className="flex items-center gap-3 text-sm font-semibold text-white"><span className="h-3 w-3 rounded-full" style={{ background: theme.color }} />{theme.label}</span>{appearance.theme === theme.key && <Check className="h-4 w-4 text-cyan-300" />}</button>)}</div><div className="mt-6 border-t border-white/10 pt-5"><label className="flex items-center justify-between gap-4 text-sm text-slate-300">Density<select value={appearance.density} onChange={(e) => update('density', e.target.value)} className="rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-sm text-white"><option value="comfortable">Comfortable</option><option value="compact">Compact</option></select></label></div></section></div>; }
