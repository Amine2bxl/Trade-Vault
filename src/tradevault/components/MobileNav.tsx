import { BarChart3, BookOpen, Calendar, LayoutDashboard, MoreHorizontal, Plus, Target } from 'lucide-react';
import { Page } from '../types';
export default function MobileNav({ page, setPage, onAdd }: { page: Page; setPage: (page: Page) => void; onAdd: () => void }) { const items: { id: Page; label: string; icon: any }[] = [{ id: 'dashboard', label: 'Home', icon: LayoutDashboard }, { id: 'journal', label: 'Journal', icon: BookOpen }, { id: 'calendar', label: 'Calendar', icon: Calendar }, { id: 'goals', label: 'Goals', icon: Target }, { id: 'analytics', label: 'Stats', icon: BarChart3 }]; return <nav className="fixed inset-x-0 bottom-0 z-40 border-t border-white/10 bg-slate-950/90 px-2 pb-[env(safe-area-inset-bottom)] pt-2 backdrop-blur-xl lg:hidden"><div className="mx-auto flex max-w-lg items-end justify-between">{items.map((item, index) => index === 2 ? <div key="add" className="relative -mt-7"><button onClick={onAdd} className="grid h-14 w-14 place-items-center rounded-full border-4 border-slate-950 bg-cyan-400 text-slate-950 shadow-lg shadow-cyan-400/20"><Plus className="h-6 w-6" /></button></div> : <button key={item.id} onClick={() => setPage(item.id)} className={`flex min-w-14 flex-col items-center gap-1 rounded-xl px-2 py-1.5 text-[10px] ${page === item.id ? 'text-cyan-300' : 'text-slate-500'}`}><item.icon className="h-4 w-4" />{item.label}</button>)}<button onClick={() => setPage('trading-plan')} className="flex min-w-14 flex-col items-center gap-1 rounded-xl px-2 py-1.5 text-[10px] text-slate-500"><MoreHorizontal className="h-4 w-4" />More</button></div></nav>; }
