{
  "name": "jarvis-server",
  "version": "0.2.0",
  "private": true,
  "description": "JARVIS — capture vocale, tri IA (Claude) vers Notion, contrôle local",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
